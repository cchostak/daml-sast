name: release

on:
  push:
    tags:
      - "v*"

jobs:
  build-and-package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install build deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install build
      - name: Build wheel/sdist
        run: python -m build
      - name: Upload Python artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: dist/*

      - name: Build Docker image
        run: docker build -t daml-sast:${{ github.ref_name }} .

      - name: Generate SBOM (Docker)
        uses: anchore/sbom-action@v0
        with:
          image: daml-sast:${{ github.ref_name }}
          format: spdx-json
          output-file: sbom.json

      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom.json

      # Optional: publish to GHCR/PyPI (requires secrets)
      # - name: Log in to ghcr
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      # - name: Push image
      #   run: |
      #     docker tag daml-sast:${{ github.ref_name }} ghcr.io/${{ github.repository_owner }}/daml-sast:${{ github.ref_name }}
      #     docker push ghcr.io/${{ github.repository_owner }}/daml-sast:${{ github.ref_name }}
